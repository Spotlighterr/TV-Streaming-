services:
  tv-streaming:
    build: .
    container_name: tv-streaming-server
    ports:
      - "8000:8000"
    volumes:
      - ./videos:/app/videos
    devices:
      - /dev/dri:/dev/dri
    environment:
      - PORT=8000
      - LIBVA_DRIVER_NAME=iHD
    restart: unless-stopped
